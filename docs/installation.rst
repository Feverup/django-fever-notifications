============
Installation
============

At the command line::

    $ easy_install django-fever-notifications

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-fever-notifications
    $ pip install django-fever-notifications
